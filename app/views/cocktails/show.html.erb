<h1 class="text-center"><%= @cocktail.name %></h1>

<h2>Voici les ingrédients pour fabriquer un <%= @cocktail.name %> </h2>
<ul>
<% @cocktail.doses. each do |dose| %>
<li> <%= dose.description  %> de <%= dose.ingredient.name  %></li>
<% end  %>
</ul>
<%= render 'shared/form', cocktail: @cocktail, dose: @dose %>
<%= link_to "Back to Cocktails", cocktails_path, class: 'btn btn-primary' %>
